# USDA SR Legacy Snapshot

The builder script can create a compact evaluation snapshot from documented
USDA SR Legacy-style values. The committed benchmark rows include source
metadata and are not generated by an LLM.
