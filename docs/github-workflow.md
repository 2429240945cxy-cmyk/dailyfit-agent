# GitHub Workflow

If `gh` is installed and authenticated, use issue branches and PRs for each
phase. If not, `GITHUB_SETUP.md` contains manual commands.

Auto-merge is intentionally off by default. Merge only after local tests and
secret scan pass.
