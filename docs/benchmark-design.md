# Benchmark Design

Benchmarks are separated from runtime prompts and samples. Nutrition ground
truth is not generated by an LLM. Guardian labels are hand-authored from public
health risk patterns. Memory queries are public/anonymized patterns.

Expected live numbers are allowed to be imperfect; suspiciously high live scores
should trigger dataset leakage review.
